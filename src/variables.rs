fn main() {
    // immutable variable
    let answer = 42;
    // mutable variable
    let mut mut_amswer = 42;

    // explicit type
    let bigint: i64 = 0;
    let float: f64 = 2.3;

    println!("answer: {}", answer);
}
